import { SliderScrollDirective } from './slider-scroll.directive';

describe('SliderScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderScrollDirective();
    expect(directive).toBeTruthy();
  });
});
